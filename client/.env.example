# Copy this file to .env and fill in values before building for production.

# Leave blank during local development — Vite's dev proxy handles it.
# When deploying, set this to your deployed backend's URL, e.g.
# VITE_API_URL=https://eventinvite-api.onrender.com
VITE_API_URL=
